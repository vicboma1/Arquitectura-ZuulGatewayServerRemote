spring:
  application:
    name: zuul-server
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true             #Arranque mediante config-service (true)
  profiles:
    active: native
